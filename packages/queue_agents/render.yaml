projects:
- name: deshmukh-neel-agents-workshop-queue
  environments:
  - name: production
    databases:
    - name: deshmukh-neel-queue-agents-db
      plan: basic-256mb
      region: oregon
      postgresMajorVersion: '18'
    services:
    - type: keyvalue
      name: deshmukh-neel-queue-agents-valkey
      plan: starter
      region: oregon
      maxmemoryPolicy: noeviction
      ipAllowList: []
    - type: web
      name: deshmukh-neel-queue-agents-web
      runtime: python
      region: oregon
      plan: starter
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.web
      healthCheckPath: /healthz
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: deshmukh-neel-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: deshmukh-neel-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
    - type: worker
      name: deshmukh-neel-queue-agents-worker
      runtime: python
      region: oregon
      plan: starter
      numInstances: 1
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.worker
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: deshmukh-neel-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: deshmukh-neel-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
